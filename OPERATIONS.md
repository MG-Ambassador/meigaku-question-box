# 現在の認証方式（2026-09-14）

運営者ごとのID・パスワード方式へ変更。メール認証・ChatGPT認証は使用しません。現行手順は [setup/PASSWORD_LOGIN.md](setup/PASSWORD_LOGIN.md) を参照してください。以下は以前の作業記録を含みます。

# 明学の質問箱：導入時の設定

現状は導入確認用。イベント名・日時・窓口・大学承認済みの個人情報取扱方針と保存期間を確定する。

## 管理権限
Sites の runtime 環境変数 ADMIN_EMAILS に承認済み担当者メールをカンマ区切りで設定。空なら全員を拒否する。管理者は ChatGPT 認証後にサーバー側許可リストでも照合する。大学 SSO は未接続。投稿にこの認証は要求しないが、Sites のサイト全体の公開範囲が非公開なら閲覧に認証が必要。

## 投稿と公開
投稿は D1 に保存し pending に固定。公開APIは published/answered の本文・カテゴリ・出典・回答のみ返す。運営画面で確認して公開・非公開・回答・削除。運営の変更は監査テーブルに記録。Instagram のリンクは /?from=instagram 。質問スタンプの収集は自動連携せず、本文だけを手動転記する。

## セキュリティ上の実装と限界
JSON本文8KB上限、質問5〜500文字、回答2000文字上限、カテゴリ許可リスト、同一Originの変更要求のみ受理、パラメータ化SQL、Reactのテキスト描画。公開投稿では信頼されたCloudflare接続IPを10分ごとにSHA-256で変換し、同じIPから10分5件に制限。識別子は次の投稿時に期限切れを削除。生IPはアプリDBに保存しない。プラットフォームのアクセスログは別管理。共有回線で制限が共用される。分散Botや大量の異なるIPへの対策として本番はWAF/ボット検証・監視・負荷試験を追加検討する。セキュリティ監査済みという保証はない。

## 本番前の運用確認
管理者の実アカウントで公開/回答/削除を確認。独立した匿名端末でログイン不要を確認。大学のセキュリティ審査、保存期間に合わせた削除手順、障害窓口、バックアップ/復旧手順を決定。サイトの所有者限定アクセスを一般公開にする作業は別途実施。現在の匿名経路はCloudflare接続IPが得られない環境では安全側に停止する。

## 複数イベント
運営画面でイベントを新規作成し、名称・日時/会場・受付状態を都度設定。管理するイベントの選択で質問一覧とInstagram転記先が切り替わる。共有URLの event パラメータで投稿先を固定。受付終了・存在しないイベントへの投稿はサーバー側で拒否する。既存イベントは名称・日時を編集可能。表示対象は最新200件、運営一覧はイベント別最新500件まで。

## メールログインへの移行準備
メール認証サービス接続前のため、現在は従来の認証方式を維持しています。Supabase Auth を利用するメール確認コード認証の実装は追加済みです。設定・配信・本番切替の手順は setup/EMAIL_LOGIN.md を参照してください。接続が完了するまで ADMIN_AUTH_MODE=email や一般公開への変更は行わないでください。
